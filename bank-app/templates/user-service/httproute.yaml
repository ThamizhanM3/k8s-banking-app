apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Chart.Name }}-user-route
  namespace: {{ .Values.namespace }}
spec:
  parentRefs:
  - name: {{ .Chart.Name }}-gateway
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: {{ .Values.user.gateway.path }}
    filters:
    - type: URLRewrite
      urlRewrite:
        path:
          type: ReplacePrefixMatch
          replacePrefixMatch: /
    backendRefs:
    - name: {{ .Chart.Name }}-user-service
      port: {{ .Values.user.service.port }}