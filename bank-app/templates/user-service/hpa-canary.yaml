apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}-user-hpa-canary
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}-user-deployment-canary
  minReplicas: {{ .Values.user.canary.hpa.minReplicas }}
  maxReplicas: {{ .Values.user.canary.hpa.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.user.canary.hpa.resource.cpu.UtilizationPercentage }} 