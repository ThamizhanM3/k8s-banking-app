apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}-frontend-hpa-stable
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}-frontend-deployment-stable
  minReplicas: {{ .Values.frontend.stable.hpa.minReplicas }}
  maxReplicas: {{ .Values.frontend.stable.hpa.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.frontend.stable.hpa.resource.cpu.UtilizationPercentage }}