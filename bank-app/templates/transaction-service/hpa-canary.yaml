apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}-transaction-hpa-canary
  namespace: {{ .Values.namespace }}

spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}-transaction-deployment-canary

  minReplicas: {{ .Values.transaction.canary.hpa.minReplicas }}
  maxReplicas: {{ .Values.transaction.canary.hpa.maxReplicas }}

  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.transaction.canary.hpa.resource.cpu.UtilizationPercentage }}