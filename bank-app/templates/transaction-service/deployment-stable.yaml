apiVersion: apps/v1
kind: Deployment
metadata: 
  name: {{ .Chart.Name }}-transaction-deployment-stable
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Chart.Name }}-transaction
    version: {{ .Values.transaction.stable.version }}

spec:
  replicas: {{ .Values.transaction.stable.replicas }}

  selector:
    matchLabels:
      app: {{ .Chart.Name }}-transaction

  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-transaction
        version: {{ .Values.transaction.stable.version }}

    spec:
      containers:
        - name: {{ .Chart.Name }}-transaction

          image: {{ .Values.transaction.stable.image.repository }}:{{ .Values.transaction.stable.image.tag }}

          resources:
            requests:
              memory: {{ .Values.transaction.stable.resources.requests.memory }}
              cpu: {{ .Values.transaction.stable.resources.requests.cpu }}
            limits:
              memory: {{ .Values.transaction.stable.resources.limits.memory }}
              cpu: {{ .Values.transaction.stable.resources.limits.cpu }}

          ports:
            - containerPort: {{ .Values.transaction.port }}

          envFrom:
            - secretRef:
                name: {{ .Chart.Name }}-transaction-secret
            - configMapRef:
                name: {{ .Chart.Name }}-transaction-config

          startupProbe:
            httpGet:
              path: /health
              port: {{ .Values.transaction.port }}
            initialDelaySeconds: 5
            failureThreshold: 30
            periodSeconds: 5

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.transaction.port }}
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.transaction.port }}
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3