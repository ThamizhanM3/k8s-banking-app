apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}-transaction-hpa-stable
  namespace: {{ .Values.namespace }}

spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}-transaction-deployment-stable

  minReplicas: {{ .Values.transaction.stable.hpa.minReplicas }}
  maxReplicas: {{ .Values.transaction.stable.hpa.maxReplicas }}

  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.transaction.stable.hpa.resource.cpu.UtilizationPercentage }}