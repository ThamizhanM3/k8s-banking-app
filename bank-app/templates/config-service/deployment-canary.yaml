apiVersion: apps/v1
kind: Deployment
metadata: 
  name: {{ .Chart.Name }}-config-deployment-canary
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Chart.Name }}-config
    version: {{ .Values.config.canary.version }}

spec:
  replicas: {{ .Values.config.canary.replicas }}

  selector:
    matchLabels:
      app: {{ .Chart.Name }}-config

  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-config
        version: {{ .Values.config.canary.version }}

    spec:
      containers:
        - name: {{ .Chart.Name }}-config-container

          image: {{ .Values.config.canary.image.repository }}:{{ .Values.config.canary.image.tag }}

          resources:
            requests:
              memory: {{ .Values.config.canary.resources.requests.memory }}
              cpu: {{ .Values.config.canary.resources.requests.cpu }}
            limits:
              memory: {{ .Values.config.canary.resources.limits.memory }}
              cpu: {{ .Values.config.canary.resources.limits.cpu }}

          ports:
            - containerPort: {{ .Values.config.port }}

          envFrom:
            - configMapRef:
                name: {{ .Chart.Name }}-config-config

          startupProbe:
            httpGet:
              path: /health
              port: {{ .Values.config.port }}
            initialDelaySeconds: 5
            failureThreshold: 30
            periodSeconds: 5

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.config.port }}
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.config.port }}
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3