apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Chart.Name }}-report-route
  namespace: {{ .Values.namespace }}
spec:
  parentRefs:
  - name: {{ .Chart.Name }}-gateway
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: {{ .Values.reporting.gateway.path }}
    filters:
    - type: URLRewrite
      urlRewrite:
        path:
          type: ReplacePrefixMatch
          replacePrefixMatch: /
    backendRefs:
    - name: {{ .Chart.Name }}-reporting-service
      port: {{ .Values.reporting.service.port }}