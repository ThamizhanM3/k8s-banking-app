apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}-notification-hpa-canary
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}-notification-deployment-canary
  minReplicas: {{ .Values.notification.canary.hpa.minReplicas }}
  maxReplicas: {{ .Values.notification.canary.hpa.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.notification.canary.hpa.resource.cpu.UtilizationPercentage }}