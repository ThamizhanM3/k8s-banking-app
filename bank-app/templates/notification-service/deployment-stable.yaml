apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-notification-deployment-stable
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Chart.Name }}-notification
    version: {{ .Values.notification.stable.version }}
spec:
  replicas: {{ .Values.notification.stable.replicas }}
  selector:
    matchLabels:
      app: {{ .Chart.Name }}-notification
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-notification
        version: {{ .Values.notification.stable.version }}
    spec:
      containers:
      - name: {{ .Chart.Name }}-notification-container
        image: {{ .Values.notification.stable.image.repository }}:{{ .Values.notification.stable.image.tag }}
        resources:
          requests:
            memory: {{ .Values.notification.stable.resources.requests.memory }}
            cpu: {{ .Values.notification.stable.resources.requests.cpu }}
          limits:
            memory: {{ .Values.notification.stable.resources.limits.memory }}
            cpu: {{ .Values.notification.stable.resources.limits.cpu }}
        ports:
        - containerPort: {{ .Values.notification.port }}
        envFrom:
        - configMapRef:
            name: {{ .Chart.Name }}-notification-config
        startupProbe:
          httpGet:
            path: /health
            port: {{ .Values.notification.port }}
          initialDelaySeconds: 5
          failureThreshold: 30
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.notification.port }}
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.notification.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3