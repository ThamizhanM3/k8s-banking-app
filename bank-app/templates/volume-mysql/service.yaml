apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: {{ .Values.namespace }}
spec:
  clusterIP: None
  selector:
    app: {{ .Chart.Name }}-mysql
  ports:
  - port: {{ .Values.mysql.port }}