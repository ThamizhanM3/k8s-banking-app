apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-mysql-service
  namespace: {{ .Values.namespace }}
spec:
  clusterIP: None
  selector:
    app: {{ .Chart.Name }}-mysql
  ports:
  - port: {{ .Values.mysql.port }}