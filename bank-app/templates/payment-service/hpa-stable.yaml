apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}-payment-hpa-stable
  namespace: {{ .Values.namespace }}

spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}-payment-deployment-stable

  minReplicas: {{ .Values.payment.stable.hpa.minReplicas }}
  maxReplicas: {{ .Values.payment.stable.hpa.maxReplicas }}

  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.payment.stable.hpa.resource.cpu.UtilizationPercentage }}