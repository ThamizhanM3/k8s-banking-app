apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}-payment-hpa-canary
  namespace: {{ .Values.namespace }}

spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}-payment-deployment-canary

  minReplicas: {{ .Values.payment.canary.hpa.minReplicas }}
  maxReplicas: {{ .Values.payment.canary.hpa.maxReplicas }}

  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.payment.canary.hpa.resource.cpu.UtilizationPercentage }}