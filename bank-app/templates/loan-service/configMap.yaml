apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-loan-config
  namespace: {{ .Values.namespace }}
data:
  DB_HOST: {{ .Values.loan.env.dbHost }}
  DB_USER: {{ .Values.loan.env.dbUser }}
  DB_NAME: {{ .Values.loan.env.dbName }}
  PORT: "{{ .Values.loan.port }}"