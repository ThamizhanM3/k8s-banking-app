apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-service-deployment-stable
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Chart.Name }}-service
    version: {{ .Values.service.stable.version }}
spec:
  replicas: {{ .Values.service.stable.replicas }}
  selector:
    matchLabels:
      app: {{  .Chart.Name }}-service
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-service
        version: {{ .Values.service.stable.version }}
    spec:
      containers:
        - name: {{ .Chart.Name }}-service-container
          image: {{ .Values.service.stable.image.repository }}:{{ .Values.service.stable.image.tag }}
          resources:
            requests:
              memory: {{ .Values.service.stable.resources.requests.memory }}
              cpu: {{ .Values.service.stable.resources.requests.cpu }}
            limits:
              memory: {{ .Values.service.stable.resources.limits.memory }}
              cpu: {{ .Values.service.stable.resources.limits.cpu }}
          ports:
            - containerPort: {{ .Values.service.port }}
          envFrom:
            - configMapRef:
                name: {{ .Chart.Name }}-service-config
          startupProbe:
            httpGet:
              path: /health
              port: {{ .Values.service.port }}
            initialDelaySeconds: 5
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.service.port }}
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3