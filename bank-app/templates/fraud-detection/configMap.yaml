apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-fraud-detection-config
  namespace: {{ .Values.namespace }}
data:
  MQ_URL: "{{ .Values.fraud.data.mqUrl }}"
  PORT: "{{ .Values.fraud.port }}"