apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}-account-hpa-canary
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}-account-deployment-canary
  minReplicas: {{ .Values.account.canary.hpa.minReplicas }}
  maxReplicas: {{ .Values.account.canary.hpa.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.account.canary.hpa.resource.cpu.UtilizationPercentage }} 