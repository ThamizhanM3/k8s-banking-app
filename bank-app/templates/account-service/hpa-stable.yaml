apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}-account-hpa-stable
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}-account-deployment-stable
  minReplicas: {{ .Values.account.stable.hpa.minReplicas }}
  maxReplicas: {{ .Values.account.stable.hpa.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.account.stable.hpa.resource.cpu.UtilizationPercentage }} 